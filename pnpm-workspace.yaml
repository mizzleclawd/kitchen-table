allowBuilds:
  # Next.js requires these locked transitive packages to build its production
  # bundle. Keep the allowlist explicit rather than approving every package.
  esbuild: true
  unrs-resolver: true
